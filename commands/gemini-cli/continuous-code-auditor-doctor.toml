description = "Diagnose a broken or not-yet-working continuous-code-auditor installation — checks config, dependencies, skill install, paths, disk, run state, and scheduling, and reports exactly what to fix."
prompt = """
Locate the installed continuous-code-auditor skill directory — check .gemini/skills/continuous-code-auditor/ in this project first, then ~/.gemini/skills/continuous-code-auditor/ — then run:

<that-directory>/scripts/commands/doctor.sh

via your shell tool, and show me its raw output verbatim. Do not interpret, summarize, paraphrase, or modify what the script prints — this is a deterministic diagnostic, not something for you to reason about.

If it reports any [FAIL] items, you may afterwards offer to help fix them, but show the unmodified output first.
"""
